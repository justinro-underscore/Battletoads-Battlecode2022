package battletoads;

import battlecode.common.GameActionException;
import battlecode.common.RobotController;

/**
 * This will be the best class I guarantee it. The meta will surely
 * reveal that this is the most useful class, and our key strategems
 * will focus around a pure sage led front. Soldier rush meta is dumb
 * and it will never work.
 *
 * @author noah
 */
public class SageRunner {

    /**
     * Run a single turn for an SAGE.
     * This code is wrapped inside the infinite loop in run(), so it is called once per turn.
     */
    public static void run(RobotController rc) throws GameActionException {

    }
}
